# Mesugak Workspace

This repository is now split into two project folders.

- `Mesugak_V1/`: preserved current implementation.
- `Mesugak_V2/`: new Mesugak architecture for multi-indicator scoring and paper trading.

Generated folders such as `node_modules`, `dist`, and `.git` were left at the repository root instead of being copied into V1.

